import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MedSearch AI — Stance-Aware Medical Literature Search",
  description:
    "Search PubMed abstracts with hybrid BM25 + semantic retrieval. Each result is classified as Supporting, Opposing, or Neutral so you understand the evidence at a glance.",
  keywords: ["medical search", "PubMed", "evidence-based medicine", "stance analysis", "NLI", "biomedical"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground antialiased">
        {children}
      </body>
    </html>
  );
}
